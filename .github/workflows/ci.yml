name: CI Build
on: [push, pull_request]  # 触发条件：代码推送或PR时运行
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: echo "Your build/test commands here"
